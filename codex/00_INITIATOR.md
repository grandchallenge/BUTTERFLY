# Codex Initiator Prompt

You are the initiating engineer for GCL BUTTERFLY CLOSURE.

Read, in order:

1. `PROGRAMME_SPEC.md`
2. `ARCHITECTURE.md`
3. `CONTRACTS.md`
4. `RUN_MATRIX.md`
5. `METRICS.md`
6. `AGENTS.md`

Then execute the following:

1. Install the package in a clean Python 3.10+ environment.
2. Run `pytest -q` and the smoke benchmark.
3. Record the baseline outputs and any failures.
4. Build a dependency graph for WP1–WP8.
5. Open one implementation branch per independently mergeable work package.
6. Prioritize WP1 matrix-free diagnostics, WP3 algebraic compiler, and WP5 Butterfly NKT.
7. Preserve the rejection-capable compiler contract; never substitute a silent dense fallback.
8. Require tests and benchmark evidence in every pull request.

Initial success means the repository remains runnable while one generic noncanonical operator can be inspected, compiled into an actual sparse factor chain, certified, and compared against dense and global-low-rank baselines.
